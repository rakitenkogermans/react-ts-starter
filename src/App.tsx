import React, {FC} from 'react';
import './styles/main.css';

type AppProps = {};

const App: FC<AppProps> = ({}) => {
    return (
        <div>
            App
        </div>
    );
};

export {App};
